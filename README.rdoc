== README

It's a simple rails app so that I wouldn't have to write all that in C (in the server that receives the data from 
physical sensor over RF24 radio). At this point the front-end is super simple, and doesn't allow to browse the data.

