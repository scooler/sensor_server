require 'spec_helper'

RSpec.describe TempMeasurement, :type => :model do
  
  describe "validation" do
    it "doesn't allow two measurements too close together (to prevent duplication when loading)"
  end
end
